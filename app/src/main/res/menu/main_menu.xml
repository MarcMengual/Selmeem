<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:constraintlayout="http://schemas.android.com/apk/res-auto"
    tools:context=".MainActivity">

    <group android:id="@+id/si"
        android:checkableBehavior="single">

        <item
            android:id="@+id/toHome"
            android:icon="@drawable/homeboto"
            android:title="Home" />

        <item
            android:id="@+id/action_pantalla_inicial_to_nova_Publicacio"
            android:icon="@drawable/nouboto"
            android:title="New Post" />
    </group>

    <group android:id="@+id/no"
        android:checkableBehavior="single">

        <item
            android:id="@+id/action_pantalla_inicial_to_perfil"
            android:icon="@drawable/perfil"
            android:title="Profile" />
    </group>

    <group android:id="@+id/op"
        android:checkableBehavior="single">

        <item
            android:id="@+id/logOut"
            android:icon="@drawable/logout"
            android:title="LogOut" />
    </group>

</menu>
